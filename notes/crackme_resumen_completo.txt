NecrumWin (Reezli challenge) - summary snapshot
==============================================

Current state:

- target sample is verified correct by hash
- visible anti-debug basics are neutralized in-process:
  - `CheckRemoteDebuggerPresent`
  - `IsDebuggerPresent`
  - `FindWindowW`
- this does not unlock the crackme by itself

Confirmed runtime chains:

Prompt:
- `0x55d9c83`
- `0x55d9c1c`
- `0x5c68c01`
- `0x5d24729`
- `0x5d2473f`

Validation:
- `0x2cf67df`
- `0x236fe1a`
- `0x23e05cd`

Late convergence:
- `0x27dd114`
- `0x2802257`

Current late-state model:

- selector chain: `R10 -> R8 -> ESI`
- recovered selector block:
  - `test r10w, 0x71ab`
  - `sete r8b`
  - `add r8d, r8d`
  - `call 0x1468d67b5`
- late gate:
  - `0x1468d67f8: neg esi`
  - `0x1468d67fa: jne 0x1461ec902`

Protection classification:

- `Initialization error 2`
  - caused by patching too early
  - currently treated as a bad initialization branch

- `bruh`
  - modal `#32770` system dialog
  - title `crackme.exe`
  - child `Static = "bruh"`
  - not success, but a trap UI

- `0xDEADC0DE`
  - underlying result of the trap route once `NtRaiseHardError` is neutralized

Most useful recent finding:

When these are neutralized:

- `NtRaiseHardError`
- `NtTerminateProcess`
- `RtlExitUserProcess`

and an early trap-producing selector route is forced, the process:

- stays alive
- avoids the popup
- changes its console title to `crackme | reezli.vc`

This is not yet a stable bypass, but it is the cleanest classified non-reject, non-popup live state found so far.

Current estimate:

- stable bypass: `79%`
- exact password: `40%`
- overall progress: `85%`

Next focus:

- avoid the `0xDEADC0DE` trap instead of only silencing its UI
- follow the live `crackme | reezli.vc` state
- move one step upward from the trap into coherent `R10`-side state production
